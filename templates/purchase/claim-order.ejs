<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ticket Confirmation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        /* background-color: #000; */
        /* color: #fff; */
        padding: 20px;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #111;
        padding: 20px;
        border-radius: 10px;
      }
      .header {
        text-align: center;
      }
      .header h1 {
        font-size: 24px;
        margin-bottom: 10px;
      }
      .header .url-button {
        background-color: #ccc;
        padding: 10px 20px;
        border: none;
        cursor: pointer;
      }
      .content {
        margin: 20px 0;
      }
      .event-title {
        font-size: 20px;
        margin: 20px 0;
      }
      .event-image {
        width: 100%;
        border-radius: 10px;
      }
      .tickets {
        margin: 20px 0;
        font-size: 18px;
      }
      .order-summary {
        margin: 20px 0;
      }
      .order-summary h3 {
        margin-bottom: 10px;
        font-size: 18px;
      }
      .order-summary p {
        margin: 5px 0;
      }
      .total {
        font-size: 18px;
        font-weight: bold;
      }
      .footer {
        margin-top: 20px;
        font-size: 12px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- Header -->
      <div class="header">
        <h1><%= data.contactName %>, you've got tickets!</h1>
        <a class="url-button" href="<%= data.orderUrl %>"> GO TO MY TICKETS </a>
        <p>View and save your tickets before the event</p>
        <p>
          Your tickets are not in this email. Access your tickets on the website
          before your event and save them on your phone for the fastest entry!
        </p>
        <!-- <p>
          To access your ticket, go to the Tickets section in your account on
          blackdiamondentertainment.com to view and download your tickets.
        </p> -->
      </div>

      <!-- Event Details -->
      <div class="content">
        <div class="event-title"><%= data.eventName %></div>
        <img
          src="<%= data.eventImageUrl %>"
          alt="Event Image"
          class="event-image"
        />
        <div class="tickets"><%= data.ticketCount %> x Tickets</div>
      </div>

      <!-- Order Summary -->
      <div class="order-summary">
        <h3>Order Summary</h3>
        <p><strong><%= data.eventName %> Ticket</strong></p>
        <p><%= data.eventDate %></p>
        <p>Order #<%= data.orderId %></p>
        <p>Order date: <%= data.orderDate %></p>

        <!-- Total -->
        <p class="total">Total $<%= data.totalPrice.toFixed(2) %></p>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>Contact us for any questions related to this purchase.</p>
        <p>
          This order is subject to Black Diamond Entertainment Terms of Service
          and Privacy Policy, and Cookie Policy.
        </p>
      </div>
    </div>
  </body>
</html>
